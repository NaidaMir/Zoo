const React = require('react');
const Layout = require('./Layout');

module.exports = function Animals() {
  return (
    <Layout >
      <h1>
        список животных
      </h1>

    </Layout>
  );
};
